# Never commit .env.
# Which client environment to run against. Valid: qa, qap (see utils/env.js).
# URL/user/password/BU/auth-file come from the client entry; the SIONIQ_URL,
# SIONIQ_USER, SIONIQ_PWD, SIONIQ_BU vars still override them if set.
SIONIQ_CLIENT=qap
